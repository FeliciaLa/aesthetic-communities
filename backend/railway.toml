[build]
builder = "nixpacks"
buildCommand = "/usr/bin/env python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn config.wsgi"
healthcheckPath = "/"
healthcheckTimeout = 100

[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"] 